import styles from "./../styles/AppBar.module.css";

export default function AppBar() {
  return (
    <header className={styles.AppBar}>
      <div className={styles.account}>Admin</div>
    </header>
  );
}
